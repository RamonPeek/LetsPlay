# Production builds are indexable by default. Set false for a staging build.
SITE_INDEXABLE=true

# Optional deployment overrides. GitHub Actions reads these from Pages settings.
# SITE_URL=https://ramonpeek.github.io
# SITE_BASE_PATH=/LetsPlay
